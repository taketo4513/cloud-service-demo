server:
  port: 9181

spring:
  # 数据库
  datasource:
    # 驱动
    driver-class-name: com.mysql.cj.jdbc.Driver
    # 连接路径
    url: jdbc:mysql://192.168.31.101:3306/user_service?useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true&useSSL=false&serverTimezone=Asia/Shanghai&rewriteBatchedStatements=true
    # 用户名
    username: root
    # 密码
    password: 1q2w3e!Q@W#E

# 日志配置
logging:
  # 指定日志配置文件位置
  config: log4j2.xml